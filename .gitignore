# Personal data — never commit
Resume/
Linkedin_Profile.pdf
data.csv

# Excel tracker — binary, changes every run, holds real scraped listings.
# Glob rather than one filename: the default is job_tracker.xlsx, but
# tracker_path is configurable and each user picks their own.
job_tracker*.xlsx

# Generated design artifacts
*.png
job-review-dashboard.html
opportunity-map.html
opportunity-map-demo.html
gen_dashboard.py
render_variant_c.py

# Tooling (local-only, not project config)
.claude/
.gstack/
.worktrees/

# Python
__pycache__/
*.pyc
*.pyo

# job_hunt package secrets + outputs (also has its own .gitignore)
job_hunt/.env
job_hunt/output/
job_hunt/*.db
job_hunt/.pytest_cache/

# Uploaded resumes — personal data, never committed.
job_hunt/data/
